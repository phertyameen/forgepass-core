# Server
PORT=3001

# Database
DATABASE_URL=postgresql://user:pass@localhost:5432/forgepass

# Auth
JWT_SECRET=changeme_in_production
JWT_EXPIRES_IN=24h

# GitHub OAuth
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=http://localhost:3001/auth/github/callback

# Stellar / Soroban
HORIZON_URL=https://horizon-testnet.stellar.org
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
STELLAR_NETWORK=testnet
BACKEND_WALLET_KEY= # NEVER commit a real key — leave blank

# Soroban contract addresses (populated after #021 deployment)
CONTRACT_PASSPORT=
CONTRACT_CREDENTIAL_STORE=
CONTRACT_TRUST_SCORE=
CONTRACT_SOULBOUND_NFT=

# IPFS / Storage
IPFS_PROVIDER=pinata
IPFS_API_KEY=
IPFS_API_SECRET=

# Anthropic (optional — AI summaries are opt-in per FR-12)
ANTHROPIC_API_KEY=

# CORS
CORS_ORIGIN=http://localhost:3000
